// El middleware en middleware.ts maneja la redirección de / según sesión activa.
// Este componente solo se muestra si el middleware falla (no debería ocurrir).
export default function Home() {
  return null;
}
